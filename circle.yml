dependencies:
  post:
    - git config --global user.email matt@friedm.com
    - git config --global user.name "Matt Friedman"
    - curl -sf -L https://static.rust-lang.org/rustup.sh | sh /dev/stdin --channel=nightly --yes
test:
  override:
    - cargo test
